import { fail, json, readBody } from "../../_lib/api.js";
export async function onRequestPost({ request, env }) { if (!env.DB) return fail("D1 binding DB is not configured.", 500); const body = await readBody(request); const orderId = String(body.paypal_order_id || "").trim(); if (!orderId) return fail("PayPal order ID is required."); if (!env.PAYPAL_CLIENT_ID || !env.PAYPAL_CLIENT_SECRET) return fail("PayPal is not configured. Set PAYPAL_CLIENT_ID and PAYPAL_CLIENT_SECRET.", 500); return json({ ok: false, error: "PayPal capture verification is scaffolded. Verify capture with PayPal before writing purchases." }, 501); }
